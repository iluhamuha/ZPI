Проект: статический сайт на Node.js + Express, без фреймворков и сборки.
Все стили — в public/css/style.css, цвета и шрифты — CSS-переменные в :root.
Меняй только то, что описано ниже. Не рефакторь остальной код, не добавляй
зависимости в package.json, не трогай server.js и другие HTML-файлы.

ЗАДАЧА: переделать секцию «Образование и дипломы» в файле public/about.html.

Сейчас это секция <section class="section section-bg"> с заголовком
«Образование и дипломы» и сеткой <div class="diploma-grid"> из шести
блоков .diploma-item с картинками-заглушками picsum.photos.
Заменить её целиком.

НОВОЕ СОДЕРЖАНИЕ — список из 8 пунктов, в этом порядке и с этими текстами:

1. Магистр психологии, Московский гуманитарный университет,
   диплом с отличием — 2005
2. Телесно-ориентированные методы развития личности — 2003
3. Эмоционально-образная психотерапия под руководством
   профессора Н. Д. Линде — 2004
4. Интегративная семейная психотерапия — 2005
5. HR-менеджер современной организации — 2007
6. Нейропсихология, МГУ имени М. В. Ломоносова — 2021
7. Нейропсихология взрослых. Нейропсихологическая реабилитация при поражениях
   головного мозга, АНО ДПО «Уральский институт повышения квалификации и
   переподготовки (УрИПКиП)», программа повышения квалификации, 72 часа — 2026
8. Ежегодная профессиональная сертификация и супервизия — с 2005 года

ВЁРСТКА:
- Каждый пункт — отдельная строка списка: слева год в моноширинном шрифте
  (--font-heading), справа текст. Год визуально выделен цветом --accent-sage.
- Между пунктами тонкий разделитель 1px цветом var(--divider).
- Порядок пунктов ровно такой, как выше (не сортируй по годам).
- Кавычки «ёлочки» и неразрывные пробелы в «Н. Д. Линde», «М. В. Ломоносова»,
  «72 часа» оставь как есть, не заменяй на прямые кавычки.

СООТВЕТСТВИЕ ФОТО И ПУНКТОВ:
Фотографии дипломов лежат в public/img/diplomas/. Привяжи их к пунктам так:
- пункт 1 → 01-psychology-2005.jpg
- пункт 2 → 02-body-2003.jpg
- пункт 3 → 03-linde-2004.jpg
- пункт 4 → 04-family-2005.jpg
- пункт 5 → 05-hr-2007.jpg
- пункт 6 → 06-neuro-msu-2021.jpg
- пункт 7 → 07-uripkip-2026.jpg
- пункт 8 → фото нет, оставь пункт только текстом

У пунктов с фото рядом с текстом — миниатюра диплома (высота ~90px,
object-fit: cover, скруглённые углы 4px, курсор pointer). Клик по миниатюре
открывает диплом в полном размере в лайтбоксе.
Обязательно пропиши осмысленный alt для каждой картинки — текст пункта.
Добавь loading="lazy" ко всем миниатюрам.

БЛОК «ДОПОЛНИТЕЛЬНАЯ КВАЛИФИКАЦИЯ»:
Сразу после списка из 8 пунктов, внутри той же секции, сделай второй блок —
я буду добавлять туда дипломы по мере появления.
- Подзаголовок «Дополнительная квалификация» (тег h3, шрифт --font-heading,
  отступ сверху ~3rem, цвет --text-muted).
- Под ним — список той же структуры и тех же классов, что и основной
  (год слева, текст справа, миниатюра справа), чтобы оба блока выглядели
  одинаково.
- Сейчас пунктов в нём нет. Поэтому оберни весь блок (подзаголовок + пустой
  список) в HTML-комментарий, чтобы на сайте он не отображался — пустой
  заголовок висеть не должен.
- Внутри комментария оставь один готовый пункт-образец, заполненный
  так: год «2024», текст «Название программы», картинка
  public/img/diplomas/extra-01.jpg. По нему я буду копировать новые.
- Перед комментарием поставь заметный маркер ровно такой строкой:
  <!-- ===== ДОПОЛНИТЕЛЬНАЯ КВАЛИФИКАЦИЯ — ДОБАВЛЯТЬ ПУНКТЫ СЮДА ===== -->
- Никаких новых CSS-классов для этого блока не создавай, кроме одного
  на отступ подзаголовка — переиспользуй классы основного списка.

ВАЖНО — ПОЧИНИ ЛАЙТБОКС:
Сейчас в public/js/main.js обработчик клика по .diploma-item ищет внутри
только тег <svg> (строка «const svg = item.querySelector('svg')») и молча
ничего не делает, если внутри <img>. Поэтому клики по дипломам не работают.
Исправь: обработчик должен работать и с <svg>, и с <img>. Для <img> —
клонируй его в .lightbox-img-container, ширина 100%, height auto.
Остальную логику main.js (форма, мобильное меню, закрытие по Escape) не трогай.

Ещё в public/about.html внизу есть статичный блок <div class="lightbox"
id="lightbox"> — он дублирует лайтбокс, который main.js создаёт сам, и в DOM
их получается два. Удали статичный блок из about.html.

СТИЛИ:
Новые классы добавь в public/css/style.css в конец секции «/* Diplomas */»,
рядом с существующими .diploma-grid и .diploma-item. Используй только уже
объявленные переменные из :root (--accent-sage, --text-muted, --divider,
--font-heading). Новых цветов не вводи. Если .diploma-grid больше нигде
не используется — оставь её, не удаляй.

АДАПТИВ:
На экранах уже 768px (медиазапрос в конце style.css уже есть) год должен
уходить на строку над текстом, миниатюра — под текст, во всю ширину колонки.

ПРОВЕРЬ ПЕРЕД ОТВЕТОМ:
- в основном списке ровно 8 пунктов, тексты совпадают с моими дословно;
- ни одной ссылки на picsum.photos не осталось в этой секции;
- клик по миниатюре открывает лайтбокс, Escape и клик по фону закрывают;
- блок «Дополнительная квалификация» закомментирован и на странице не виден,
  но маркер-строка в коде на месте;
- на 375px ничего не вылезает за экран.

В ответе перечисли изменённые файлы и что именно поменял.